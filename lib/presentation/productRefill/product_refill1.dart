import 'package:flutter/material.dart';

class ProductRefill1 extends StatelessWidget {
  const ProductRefill1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('refill 1'),
      ),
    );
  }
}
