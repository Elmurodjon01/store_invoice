import 'package:flutter/material.dart';

class ProductRefill2 extends StatelessWidget {
  const ProductRefill2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('refill 2'),
      ),
    );
  }
}
